# OpenAI API Configuration (Optional - system works in offline mode without this)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Local/Offline Mode Configuration
# Set to true to use the built-in rule-based system without any API calls
# This allows the system to work completely offline without any external dependencies
LOCAL_MODE=false
